name: popularity_on_movielens_ctr
benchmark: conf/benchmarks/movielens_ctr.yaml
algo: conf/algorithms/popularity.yaml
seeds: [1, 2, 3]
# Popularity is a classical algorithm: the runner bypasses Lightning
# entirely and calls ``algo.fit`` directly. The ``trainer`` block here
# is only consulted for ``limit_val_batches``, which keeps the
# ranking-metric smoke evaluation cheap.
trainer:
  limit_val_batches: 4
